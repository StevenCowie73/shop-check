#!/bin/sh
# Refuses a commit that carries a real business or person's name.
# Installed by: npm run hooks:install
# Bypass once (think first): git commit --no-verify

root=$(git rev-parse --show-toplevel) || exit 0
exec node "$root/scripts/check-private-names.js" --quiet
